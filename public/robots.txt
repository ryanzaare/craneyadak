User-agent: *
Allow: /

# فایل‌های داخلی موتور جستجوی Pagefind (شاخص‌های باینری/JSON) ارزش ایندکس
# ندارند و فقط بودجه‌ی خزش مصرف می‌کنند.
Disallow: /pagefind/

# نکته‌ی عمدی: صفحه‌ی /search و /thank-you این‌جا Disallow نشده‌اند. هر دو با
# متاتگ `noindex` علامت خورده و از sitemap حذف شده‌اند؛ اگر مسیرشان
# Disallow می‌شد، گوگل اصلاً آن‌ها را نمی‌خزید و در نتیجه متاتگ noindex را
# هرگز نمی‌دید — یک تناقض رایج که دقیقاً برعکس هدف عمل می‌کند. ضمناً
# مسدودکردن /search با الگوی پارامتر، با SearchAction تعریف‌شده در اسکیمای
# WebSite صفحه‌ی اصلی هم در تعارض قرار می‌گرفت.

Sitemap: https://craneyadak.com/sitemap-index.xml
